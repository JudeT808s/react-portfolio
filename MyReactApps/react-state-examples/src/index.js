import React from 'react';
import ReactDOM from 'react-dom/client';
import Clicky from './examples/Clicky'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Clicky />
  </React.StrictMode>
);